language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t lopezjavier/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true lopezjavier/docker-react npm run test -- --coverage